---
- name: Download slipstream-client binary
  get_url:
    url: "{{ slipstream_client_binary_url }}"
    dest: /usr/local/bin/slipstream-client
    mode: 0755
  tags:
    - slipstream-client-binary
